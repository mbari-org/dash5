# Widget data host (used by VehicleDiagram / VehicleList to fetch /widget/auv_*.json).
# Must be NEXT_PUBLIC_ so the variable is accessible in the browser bundle.
# Dev:  http://localhost:3002  (api-proxy.js forwards to okeanids.mbari.org)
# Prod: https://okeanids.mbari.org  (or wherever the widget JSON files are served)
NEXT_PUBLIC_API_HOST=http://localhost:3002
NEXT_PUBLIC_BASE_URL=http://localhost:3002/TethysDash/api
NEXT_PUBLIC_ODSS2BASE_URL=http://localhost:3002/odss2dash/api
NEXT_PUBLIC_WEBSOCKET_URL=wss://okeanids.mbari.org/ws/
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=<your key here>
NEXT_PUBLIC_ESRI_API_KEY=<your key here>

# ── MBARI SSO (Microsoft Entra ID) ──────────────────────────────────────────
# App registration created by Andrew McCann, valid 90 days from ~Jun 2026.
# Ask Andrew for a new secret before it expires or request a long-lived one.
#
# Client ID is the same for all environments of the sinkerdev registration.
NEXT_PUBLIC_MSAL_CLIENT_ID=2f3b2230-be51-46c5-bac0-2e887bfea35c
#
# Redirect URI must match exactly what is registered in Entra ID.
# NOTE: The original registered URIs were .../sinker/api/auth/callback/mbari
# but this is a static-export app with no server-side handlers.
# Ask Andrew to UPDATE the registered redirect URIs to:
#   https://sinkerdev.shore.mbari.org/sinker/   (sinkerdev)
#   https://sinker.shore.mbari.org/sinker/       (prod)
# Then set this variable accordingly per environment.
NEXT_PUBLIC_MSAL_REDIRECT_URI=https://sinkerdev.shore.mbari.org/sinker/