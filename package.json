{
  "name": "mbari",
  "version": "1.0.0",
  "description": "Web based UI and client applications for MBARI.",
  "repository": "https://github.com/SumoCreations/mbari.git",
  "author": "Sumo Creations LLC",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": "14.x.x",
    "yarn": "1.22.x"
  },
  "scripts": {
    "prettier": "npx prettier --write packages/*/src/**/*.{js,md,ts,tsx}",
    "build": "./scripts/bootstrap.build",
    "build-test": "./scripts/test.build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.21.1",
    "turbo": "^1.1.10"
  }
}
